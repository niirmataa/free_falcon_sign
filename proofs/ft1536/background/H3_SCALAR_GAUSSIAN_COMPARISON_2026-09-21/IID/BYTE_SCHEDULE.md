# Original getters: exact bytes, drops i zasoby

Source internal.h767–778/814–866, literalny build x86_64 LP64. prng.buf.d ma4096
bytes, aligned union, ptr:size_t, opaque state256bytes,type int. Legal ptr0..4095.

## Pojedyncze gettery

get_u64: przed odczytem sprawdza **ptr>=4087**, nie >4087 i nie >=4088.
Gdy true, opuszcza4096−ptr bytes (1..9), refills i lokalne u=0. Gdy false,
u=ptr≤4086. Zapis ptr=u+8∈[8,4094]; read[ u,u+8 ) zawsze w buforze.
FALCON_LE_U używa memcpy8 do uint64, równoważnie little-endian Σbyte_i*256^i;
pozostała gałąź robi jawne shifts. Źródło nie zastępuje tego get_bytes.

get_u8: czyta byte[ptr], increment ptr; jeśli osiągnie4096, refill PO odczycie
i return starego byte. Wtedy drop0. Word/byte getters wybierają wyłącznie nowe
positions, każdy konkretny word ma dokładnie jedno little-endian8-byte encoding.
Range/shift/carry lemmas są w IidBytes.lean; pełne4096-entry pointer transitions
w BYTE_SCHEDULE.json są exact finite certificate, nie statistical fixture bound.

## Legalna proposal

Source order: hi:u64,lo:u64,b:u8,mask_word:u64,threshold_word:u64.
BerExp zawsze pobiera oba words, również gdy mask albo cutoff już odrzuca.
To **33 returned bytes**. Po drugiej u64 ptr≤4094, zatem sign u8 nie refilluje.
Całe5-operation sequence może zrobić najwyżej jeden refill (po resecie pozostałe
≤33 bytes nie osiągną4087). Po proposal ptr q∈[8,4094]. Dla start p:

```
R∈{0,1}, 0≤D≤9R, p+33+D=4096R+q.
```

Indukcja po n≥1 completed proposals daje total R≤n,D≤9R i conservation
p+33n+D=4096R+q. Ponieważ p≤4095,q≥8:
4087R≤4087+33n, więc **R≤min(n,1+floor(33n/4087))**.
Counts zależą tylko od p i n, nie od outcomes/tape poza długością rejection.
Cały actual event z N=n ma ten sam q_n,R_n,D_n. Nie ma resetu PRNG co proposal
ani co scalar call. Entry block jest liczony osobno jako wcześniej wygenerowany.

## Conditional IID resource export

Dla N z REJECTION_LAW: returned=33N,discarded=D_p(N),new refill bytes=4096R_p(N).
W IID_BUFFER, conditional PAST:

| Wielkość | bound średniej |
|---|---|
|N|256|
|returned bytes|8448|
|new refills|12535/4087|
|discarded bytes|112815/4087|
|new generated bytes|51343360/4087|

Do wygenerowanego entry block można dodać4096, jeśli zasób ma obejmować także
inicjalizację. Dla ghost event N≤m używa się exact q_m,R_m,D_m lub powyższego
boundu, a complement kosztuje ≤(255/256)^m w grze IID. Nie jest to nowy source
cap/abort. Dla s legalnych adaptacyjnych calls consumer może użyć conditional
tower/union bound s(255/256)^m bez independence, po osobnym wykazaniu call-count
i entry invariants. Nie nadajemy przez to statusu pełnej ordered joint law.

Native normal/sanitizer kontrolują wszystkie4096 start ptrs, dodatkowo u8 przy4095,
pełne returned words/endian/pointers/refills/drops. Finite-prefix exhausted fixture
ma osobny tag HARNESS_EXHAUSTED/NONE, nie return0 źródła.
