# Kontrolowane kontrmodele i ich membership

Nie stwierdzono required-domain counterexample do nowego tail theorem.
Poniższe wyniki falsyfikują określone skróty,nie schemat kryptograficzny.

1. **Raw L=ideal c/a:** synthetic constant rounded basis[64,32,200,24833/64]
   ma determinant18433,source root L=div_C(25216.5,5120) różny od exact
   50433/10240. Source words i exact rational difference zachowano. Nie jest
   P_key/Emitted key (f/g nie ternary). To local actual-division countermodel.
2. **A2/paired as ordinary Euclidean/stored variance:** inverse A2 coordinate
   diagonal4/3 oraz source paired sigma powodują różne exact local row proxies.
   Pominięcie paired factor underestimates variance w kontrolowanym przykładzie.
3. **Zero source error:** full native+RBF map ma niezerowy error (constant fixture
   około6.1e-13). Dowodzi potrzeby error term; nie jest świadkiem przekroczenia
   E1095 lub samego1/128 w wymaganej domenie.
4. **Discrete normalizer ratio1 / exact mean-zero / independent innovations:**
   local adaptive terminal Q_S reference ma nonzero conditional means,
   joint MGF różne od iloczynu marginals i Z(0,v)/Z(1/2,v)>1. Kontrole używają
   actual source support/centers,widths,ale nie mają emitted root membership.
5. **Truncation factor1:** dodatkowy EXTENDED toy support{0,1} daje conditional
   MGF większe od full Gaussian MGF. Refutuje generic rule bez1/(1-t),nie
   twierdzi required source violation dla konkretnej błędnej majoranty.
6. **Jeden vector / złe ties:** standalone pre-cast a0,b32768 jest joint bad;
   first-vector-only event go gubi.−32768.5 source rint jest safe−32768,
   +32767.5 daje bad32768. Native controls i exact dyadic oracle potwierdzają
   również sąsiednie words; te syntetyczne arguments nie są actual root traces.
7. **Reverse chi2:** exact two-point laws P(B)=1/5,Q(B)=1/4 mają forward1/75,
   reverse1/64. Skopiowanie forward boundu do reverse jest fałszywe w tym
   abstract probability countermodel. JOINT small bound ma wyłącznie forward
   direction; nie przypisujemy tego toy countermodelowi emitted membership.

Pełne12 detected mutations i no-op mają baseline/changed values,sha/scope
w artifacts/mutations. Żadnej fikcyjnej deklaracji wykrycia na domenie, której
nie obejmuje test. Coarse error>=1margin jest odrębnym luźnym boundem,nie witness.
