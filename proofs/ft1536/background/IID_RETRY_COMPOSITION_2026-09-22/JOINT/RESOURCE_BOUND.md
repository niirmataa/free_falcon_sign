# Jedna root próba — zasoby w IID_BUFFER

M=3072 wynika z SOURCE_ORDER. T=Σ_i Ni, Ni≥1. Wszystkie twierdzenia są
conditional on dowolna legal entry PAST, bez future norm acceptance.

## Expectation i union bound bez independence

Z conditional geometric tail `Pr(N_i>m|PAST_i)≤(7/8)^m`, monotone convergence
i sumy geometrycznej E[Ni|PAST_i]≤8. Tower i skończona suma dają E[T]≤24576.
Jeśli wszystkie Ni≤m, toT≤Mm; union/tower dają
Pr(T>Mm)≤Pr(∃i:Ni>m)≤3072(7/8)^m. Dla m768 jest to<2^-136,
z konserwatywnym T cap2359296. Ten luźniejszy bound zachowano.

## Ostrzejszy conditional moment bound

Dla1<z<8/7 i A≥1/8:
E[z^Ni|PAST_i]=A z/[1-(1-A)z]≤z/(8-7z).
Denominatory positive. Cross multiplication: dla p=1/8≤A różnica numeratorów
odpowiedniego upper-minus-lower jest(A−p)z(z−1)≥0; kernel sprawdza cleared
identity. To conditional inequality dla rzeczywistych adaptacyjnych Ai.
Z_(i−1)=z^(Σ_<i Nj) jest PAST_i-measurable; tower krok po kroku daje
E[z^T]≤[z/(8-7z)]^M, bez postulowania niezależnych geometric variables.

Markov dla z17/16, Tcap16M=49152:

```
Pr(T>49152) ≤ [(17/9)*(16/17)^16]^3072
           < (3/4)^3072 < 2^-1024.
```

Ostatnie exact rational inequalities: factor<3/4 i(3/4)^3=27/64<1/2,
M=3*1024. RESOURCE_BOUND.json i JointResource instancjują je bez float logs.

## Literal getter conservation

Źródło internal.h: get_u64 refill PRZED read przy ptr≥4087, drop4096−ptr∈[1,9],
potem8 little-endian bytes; get_u8 read/increment/refill PO odczycie przy4096.
Proposal sequence8,8,1,8,8 zwraca33bytes, ma≤1refill i endptrq∈[8,4094].
Nie ma resetu pomiędzy proposal ani scalar calls. DlaT≥1:

```
p+33T+D=4096R+q, D<=9R,
R<=min(T,1+floor(33T/4087)).
```

Dowód: sumowanie per-proposal conservation; p≤4095,q≥8 daje
4087R≤4087+33T. Kernel conservation_compose/repeated_getter_bound zamykają
integer step. Exact4096-start transition table i native controls są świeże.
R,D,q zależą tylko od p,T, a partial-call endptr od prefix sumNi. Opaque PRNG
state nie jest modelowany jako source of independent randomness w real code.

Na ghost T≤49152: returned≤1622016 bytes, new refills≤397,
discarded≤3573, new generated bytes≤1626112; z wcześniej wygenerowanym
entryblock≤1630208. Uniform expectation: returned≤811008,
E[R]≤1+811008/4087; E[D]≤9E[R], E[newbytes]≤4096E[R].

Te ghost resource caps są ZDARZENIAMI ANALIZY. Oryginalny C nadal nie ma takiego
proposal capu/abortu. Przy real finite-resource transcript comparison należy
utrzymać exhaustion tag odrębnie i ponieść jego IID tail; nie conditioning
bez normalizacji. Nie dowiedziono tutaj real-PRNG indistinguishability,
real root/Sign termination ani globalnego runtime/constant-time boundu.
