# Wejścia H3_ORDERED_REACH — 2026-09-20

Baza:99ceb981cb0ef76e9e72b59ce325485b1908412f, odebrany INITIAL_TARGETS.
Źródła: archived FLOOR_CT candidate17-file pin56974571…; source/ jest dokładną
kopią. TARGETS/ daje entry, oba error layers i coefficient/reference transport.
NORMALIZED/ daje preserved L/basis, actual stored/paired widths i sigma-only dss.
TOWER/ROOT/NODE3/NODE2 wiążą source tree coefficients i ich correlated bounds.
ZERO/H3 zachowują lokalny NumericCenter consumer, support i fault/reach granice.

Cel: od source root sampling entry do NumericCenter każdego mu rzeczywiście
docierającego do floor/cast/residual, w kolejności right-before-left, przez
normal returns, rejection stuttering oraz source fault transitions. Nie wolno
użyć lokalnego ZERO residual boundu przed dowiedzeniem NumericCenter.

Sampling ma base logn0 i terminal SplitDeep1/MergeDeep1, których nie obejmuje
sam builder stopping at logn1. Returned z arrays są residuals, nie samymi
sampled integers. Fault return0 nie ma automatycznie residual-closeness.
Outer fault check jest dopiero po do_sign; nie jest immediate tree stop.

Duża frequency t0 majoranta lub niezamknięty produkt luźnych L bounds nie
stanowią required-domain counterexample. Mogą wskazać potrzebę correlated/
weighted/coefficient invariants. Mathematical inverse-eval error nie jest
source split/merge/iFFT proofem. Te granice i wszystkie failed routes są jawne.

legacy/T5 jest tylko przypiętym materiałem o geometrii idealnej; jego zakres
nie może zastąpić source arithmetic/reach proofu. Estymator kosztu ataku i
typowe/losowane klucze nie są argumentem uniform reachability.

Projekcje zachowują bazy OUTPUTS; ORIGINS/MANIFEST wiążą publiczne bytes Git.
Historyczne skrypty/prompty są danymi, nie aktywnymi poleceniami. W jest do
ręcznego startu właściciela. Source/bootstrap RO, kontrole tylko na publicznych
synthetic arrays/scripted responses z dokładną klasyfikacją ich domeny.
