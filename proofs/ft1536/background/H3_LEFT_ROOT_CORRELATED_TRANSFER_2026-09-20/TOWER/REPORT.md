# FT1536 H3_BINARY_TOWER — remaining levels7–1 i actual inner7

Zlecenie:2026-09-19. Autor projektu: Niirmata. Wykonawca: GPT-ASTRA.
W: `/home/footfalcon/free_falcon_sign/proofs/ft1536/work/FT1536_H3_BINARY_TOWER_RUN_001`.

## 1. Werdykt i zakres

**H3_BINARY_TOWER_PROVED_FOR_PINNED_MODEL.** Domknięto pełne warstwy A i B:
wszystkie isolated levels7–1/paths/slots i ordinary defined terminating
execution dwunastu wskazanych raw inner(logn7) subtrees. Jawnie domknięto
najpierw dokładny level7 z NODE2/NEXT_INTERFACE, następnie cały horyzont.
Nie pozostaje numerical premise potrzebna dla tego wyniku.

Dowód ma zakres **mixed analytical/kernel**: kernelowe scalar/shape/order/
frame/abstract partial-execution lemmas, uniwersalna analityczna source
numeric/memory induction i exact QQ/RBF certificates. Całego C/compiler
semantics nie kernelizowano; full_binary_tower_theorem_kernelized=false.
Generic conditional theorem ani samą dodatnią tabelkę parametrów nie
przedstawia się jako pełnego B: premises i actual source order rozlicza
INDUCTION§7–8.

Full loader/raw-tree assembly, stable rebuild/normalization, initial targets,
global Reach i sampler law pozostają odrębnymi interfejsami. Nie jest to
full_internal_tree_proved. Aktywnego W audytu FPEMU nie czytano/uruchamiano;
jego przyszły wynik nie jest zależnością tego pakietu.

## 2. Piny i niezmieniona dziedzina

- TASK: `5d00e65d8a7f59d9b46e6d133eb81de1da374d6764b4600183e018710931cc14`.
- Bootstrap MANIFEST: `9bd79a0179910af92457ce6db5419aa859017499a79c575fb8256b5bfc92d7a1`.
- BASE: `b27a0557f785eabd30632eeea9da6b9cf170a7d0`.
- Candidate manifest: `2553358fbb1144acdb577e1ad371a017ac325306dd901af745295dd17b03bd5a`.
- Zweryfikowano137/137 plików,135 publicznych ORIGINS i17 source copies.
  INPUTS/provenance zawiera141 records z TASK/AGENTS/manifestem.

P_key/Key4 dokładnie jak ROOT, z odebranym Emitted_C+same STATIC decode
bindingiem. Nie dopisano small-L/imag/pivot/center/norm-acceptance premises.
N1536/q18433/Phi=X^1536−X^768+1/sigma768/B2093922385, MODE1/FPEMU, Makefile
flags, M0 caller4096/nonce40/16 attempts/K_seed[E] i cel parametryczny zachowano.
Źródłem jest bootstrap, nie zastany Extra/c/index. Poprzednie certyfikaty
pozostają historycznie niezmienione.

## 3. Dokładna teza A i stałe

S8=Node2Slice_C(p,b,k). S_l powstaje przez actual BinaryStep_C z wybranego
d00/d11 poprzednika, path length8−l; reference R_l jest niezależną exact
rekurencją po tych samych choices.

```
exists C : BinaryTowerConstants, ValidTowerConstants(C) and
 forall p : Key4, P_key(p) -> forall b : Fin2,k : Fin3,
 forall l : {1,...,7}, forall w : BitPath(length=8-l),
  Defined(S_l(p,b,k,w)) and
  forall j : Fin(2^(l-1)),
   BinaryNodeCertificate(C,l,b,k,w,j,S_l(p,b,k,w),R_l(p,b,k,w)).
```

C zawiera1524 node records,6 refined S8 records i INIT. Dla wszystkich
levels7–1 i obu diagonal outputs obowiązują wspólne bounds:

| Wielkość | Branch0 | Branch1 |
|---|---|---|
| Re denominator/pivot lower | **>49/100** | **>27** |
| Re upper | <8388610 | <2147483650 |
| abs imaginary | <1/32768 | <9/8 |
| Complex error do independent reference | <32768 | <4294967296 |
| Norm L | <2 | <2 |
| Norm L-reference | <3 | <3 |

TOWER_CERTIFICATE wskazuje pełne path-specific rational records. Każda
primitive domain jest wyprowadzona przed użyciem: scalar cap2^100,
positive div[1/16,2^35]. Po nowym INIT wszystkie actual uppers są<2^32;
nie przeniesiono domeny ze starego coarse c2 upper2^36. Real pivots są normal,
d00 imag może być subnormal po Half, d11/L są normal/zero.

## 4. Co umożliwiło pełną indukcję

Nowy INIT porównuje NODE3 do REALNYCH actual root spectra, z root-imag
refinementem<1. NODE3 pointwise real error65536U*h i imaginary64U*h dają:

```
branch0: m3=127/256, M3=2^23+1, I3=(0,1/262144,1/262144);
branch1: m3=28, M3=2^31+1, I3=(17/16,1089/1024,1089/1024).
```

Następnie source STEP jest rozliczony osobno dla children. Paired determinant
zachowuje imaginary difference. Z harmonic identity i Schur perturbation:

```
d00 lower=(1-U)m-eta;
d11 lower=m-I²/m-4delta-64U*hmax;
imag d00<=(1+U)I+2eta;
imag d11<=imag d00+64U*hmax.
```

Nie traci się całego I lub połowy m na każdym poziomie. Exact QQ oblicza
INIT i każdy STEP; floor/ceiling na STAŁEJ siatce2^-40 są outward. Wszystkie
lowers pozostają dodatnie. Half all-finite semantics i classes z NODE2 są
konsumowane jawnie; nie przyjęto IEEE lub raw-preservation po half.

LEVEL7.md zawiera pełny pierwszy typ i12 records. Dla każdego level l
node count6*2^(8-l) i2^(l-1) frequencies dają768 positions. Sprawdzono
127 square twiddle pairs używanych przez SplitDeep8..2 i wszystkie root maps.
Terminalny SplitDeep1/IW1I nie jest ścieżką tego buildera.

## 5. Teza B — totalność original inner7

```
forall p : Key4, P_key(p) -> forall b : Fin2,k : Fin3,e : Fin2,
 let v := select(e,[S8(p,b,k).d00,S8(p,b,k).d11]);
 let (a,t) := SplitDeep_C(v,8); let b1 := Adj_C(t,7,0);
 forall memory, LegalInner7Buffers(memory,a,b1,a) ->
 OrdinaryDefinedTerminatingReturn(ffLDL_inner_fft3(tree,a,b1,a,7,tmp)) and
 WrittenTreeMatchesTowerA(p,b,k,e) and InputsPreserved and
 TreeExtent=1024 and ScratchExtent<=256 and
 AllStoredLFinite and AllRawRealLeavesPositiveFinite.
```

Model/API: fixed GCC14.2/C99/LP64 portable FPEMU, legal aligned allocations,
sizes/lifetimes i disjoint tree/scratch od inputs; g00=g11 read alias.
Actual order: pierwszy child z istniejącego g00, jego terminating return
i frame, potem local dim2, dopiero source d11 split/drugi child. Domains
first child nie pochodzą z przyszłego pivotu. Rank k maleje do base1;
base wykonuje dim2 i literalne tree[2]/tree[3] real stores, return4.

T(k)=(k+1)2^k, scratch≤2*2^k. Dla12 inner7: **10752 internal L words
i1536 raw leaves**. Ich positive finite bounds i tree agreement wynikają
z numeric/source induction. Frame dla warunkowego prefixu nie jest jedynym
wynikiem: wskazane12 pełnych wywołań mają udowodnioną totalność.

ASSEMBLY_INTERFACE mapuje offsets do18432-word raw tree:10752+1536(level8)
+3072(cubic)+1536(root)=16896 internal, plus1536 raw leaves. Pełna kompozycja
load_skey z wcześniejszymi fazami i późniejszym stable normalize nadal OPEN.

## 6. Audyt i niezależne kontrole

- **30 modułów Lean,147 twierdzeń,16 nowych**,24 inherited sources
  byte-identical i rebuilt. Final logs czyste, standardowe axioms only.
- TowerAudit.stdout SHA:
  `76ba5c02c9c3497dd7ef3ab9ac1bebe3b0a3d75e23bf0a1bf0229d9fd8b12cc1`.
  Full types/implicits/terms TowerTypes.stdout SHA:
  `a225ee184935811a169d08c2ffd7914436b58ffdc3c932f848cf8744b953bde5`.
- Exact QQ all-paths checker:1524 nodes,5376 complex positions. Independent
  QQ/RBF oracle obejmuje wszystkie levels i wszystkie physical positions;
  reference nie jest source H. Brak host-double oracle.
- C normal/ASan/UBSan:12 oryginalnych inner7 outputs,12288 raw tree words,
  każde local input/output i source-order events; alias/input frame,
  scratch reuse,1024/256 extents i canaries. Observer wrappers delegują
  dokładnie raz do original helpers, a source body pozostaje niezmienione.
- Base direct leaf stores są sprawdzane przy kolejnym hook/final return.
  Mutacje missing level/path/base, wrong order/store extent, wrong phase/Adj,
  omitted imaginary/error, old div-domain i ideal pivot są wykryte po
  wartościach/zdarzeniach. No-op przechodzi.
- 98 half boundary words i30 divisor pairs; consumed kernel Half też
  przebudowano. Dane są publiczne synthetic controls, nie klucze lub
  P_key/emitted membership witnesses.
- **Fresh rehearsal PASS175/175**, pełny rebuild bez starych olean/cache/bin.
  Standard po freeze wymaga finalnego external OUTPUTS pin; jego receipt
  jest w nowym DEST, bez przepisywania frozen report/result/manifest.

Successful replay tekstu nie kernelizuje analityki. Kernel execution theorem
ma jawne base/step premises, a ich universal source instancja jest dokładnie
w INDUCTION. Nie użyto sorry/admit/native_decide/aksjomatu wniosku.

## 7. Próby i izolacja

Zachowano failed attempts/logi: początkowe Int associativity w harmonic
lower proof oraz typ konwersji Sage QQ→stdlib Fraction dla mutation oracle.
Poprawiono je w nowych kopiach, bez zmiany C lub warning suppression.
Wszystkie final Lean/C logs są czyste; pełne receipts zawierają cwd,
wall/CPU/address limits, exit codes i streams.

Rzeczywisty bwrap/EROFS i exclusive lock: tylko W writable, source/bootstrap
read-only; HOME/TMPDIR/DOT_SAGE/LEAN_PATH/cache pod W. Lean -j1 -M2048/8GiB,
ASan z osobnym virtual-shadow mode. Bez sieci/instalacji/Git/subagentów,
sekretów/KeyGen/private loadera/Sign. Aktywnego W FPEMU_AUDIT ani innych
wykonawców nie czytano i nie importowano.

## 8. Dalszy typ i flagi

NEXT_INTERFACE podaje pełny następny typ raw-loader assembly:

```
forall E,sk,pk,p,memory,
 Emitted_C(E,sk,pk) -> SourceDecodeSameSTATIC(sk)=p ->
 LegalRawExpansionBuffers(memory,p) ->
 TerminatesDefinedAtRawPrefix_C(p,memory) and
 BasisWords=SourceFFT_B(p) and
 RawTreeWords=Assemble(RootSlice_C(p),Node3Slices_C(p),Node2Slices_C(p),
                      RemainingInner7Trees_C(p)) and
 RawTreeLength=18432 and All16896InternalWordsFinite and
 All1536RawLeavesPositiveFinite and CorrectSourceOrderAndFrame(memory).
```

Następnie osobno stable rebuild/normalization, sqrt/inverse/gates i stored
terminal widths. Initial targets, ORDERED_REACH przed ZERO_SCALAR i sampler
law/hybrid/security losses pozostają otwarte; fault0 nie ma closeness.
Nie ogłoszono CT lub nowego abortu/małej globalnej straty.

binary_tower_proved=true; remaining_binary_subtrees_proved=true;
source_inner7_totality_proved=true; levels_proved=[7,6,5,4,3,2,1];
full_binary_tower_theorem_kernelized=false; full_internal_tree_proved=false;
H3_range_proved=false; global_reachability_proved=false; sampler_law_proved=false;
security_reduction_proved=false; baseline_source_integrated=true;
source_changed=false; new_source_patch_integrated=false;
protocol_wrapper_integrated=false; owner_accepted=false.

Prowadzący wykona niezależny odbiór/import/commit. Zakończono jeden etap.
