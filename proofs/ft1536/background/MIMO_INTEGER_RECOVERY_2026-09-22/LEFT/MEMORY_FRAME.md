# Read moments, nowe proof objects i source frame

Dziedziczymy exact root entry: sk24576 fpr words, tmp10752, t0=tmp0,t1=tmp1536,
z0=tmp3072,z1=tmp4608,tree=sk6144,scratch=tmp6144. Typed/aligned/lifetime/
disjointness i legal scalar PRNG/context byte reads są jawnymi premises.
Normalizacja zachowuje raw L i basis; stored widths pochodzą z tree, nie
ze starych tmp leaves. Proof objects Q_Re(v),T_C,Z*,signal cU nie są runtime
writes ani dodatkowymi inputs C.

Raw metrics odnoszą się do immutable key-construction snapshots. RAW/TOWER
wiążą local L i real pivots w momencie ich powstania; stable source D
wiąże NORMALIZED emitted certificate z pełną reversed leaf map. Nie wymagamy
zachowania martwego construction scratch po loaderze.

W sampling prawa recursion nie nadpisuje root t0/t1 ani tree. Po jej return
z1 jest immutable dla left recursion, więc CM(z1,Lroot) przy1819 i1837 ma
te same operands/word output. Analogicznie lower/cubic repeated products są
zachowane przez source frame; source add/sub still have distinct errors.
Każdy split czyta live input przed reuse outputs; cubic copy1765 pozostaje.
Terminal0 czyta oba targets i width przed locals/update/callbacks.

Source memory bounds pozostają S_inner(k)=2^(k+1)−2, depth9 scratch1022,
top peak8702<10752. Conditional defined-prefix frame zachowuje tree/key/hm,
root targets i tmp tail8702..10751, a callback zapisuje tylko legal context.
Poza tym nie deklarujemy initialized przyszłych arrays przy nonreturn.
Nowy proof domyka numerical domains active left prefixes przed operations;
nie nadaje totality arbitralnemu faulted tail lub postprocessingowi.

Native controls budują tree oryginalnym ffLDL, liczą stable/normalize na
publicznych roots, potem wykonują tylko oryginalną sampling recursion z
scripted relation. Pełne arrays/canaries/word snapshots są porównywane.
Nie wykonano private loadera, KeyGen, całego do_sign/Sign lub private data.
