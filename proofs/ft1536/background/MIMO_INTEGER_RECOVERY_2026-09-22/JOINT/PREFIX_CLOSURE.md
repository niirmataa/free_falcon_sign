# Positive-support prefix closure PRZED kolejnymi instructions

## 1. Wymagane entry i brak koła

E to dokładnie required root entry ORDERED/LEFT: legalny M0 E, Emitted lub
same-STATIC decode, normalized source sk z immutable actual raw L/basis,
canonical target c i zdefiniowane source initial targets, reset faultNONE,
typed/aligned/disjoint memory/context/getter interface. IID_BUFFER dodaje
conditional unread-tail/future-block law. Nie zakładamy przyszłego NumericCenter,
braku fault w całej trace, norm acceptance lub zwrotu rejection loops.

H0={empty}. Jeśli h∈Hi, wykonujemy już uzasadnione caller prefix operations do
current scalar cut, wyprowadzamy jego domenę, obliczamy finite S_h z actual
table i BerExp. Dopiero potem definiujemy H_(i+1)={h·y:h∈Hi,y∈S_h}. To nie
definicja legalności przez future safe execution: istnienie next cut i jego
current domain są WNIOSKIEM następującej witness induction.

## 2. Każdy y∈S_h ma skończony normal source witness

Po udowodnieniu current NumericCenter i NORMALIZED width/dss, IID daje exact
proposal intervals i zero-aware s_C. Map(k,b)→y=s_C−k albo s_C+1+k jest bijekcją
z N×{0,1} na Z. GAUSS daje **S_h: n_j,k>0 AND e_C(k,b)<64** i Z_C>0.
Dla y∈S_h wybierz U128=T_j[k], dolny endpoint integer interval
[T_j[k],T_j[k−1]) (T_j[−1]=2^128); strict table comparisons zwracają ten k.
Wybierz sign byte o parity b, oraz oba BernExp words równe0. Wtedy low-e mask
jest0, over=0 i0<Z, więc source akceptuje w PIERWSZEJ proposal i zwraca y.
Wszystkie shifts/casts/operations są legalne z current domain, nie dzięki temu
przyszłemu return. Raw33 bytes mają probability2^-264 w conditional IID game.

Od KAŻDEGO legalnego ptr literal getters dają rozłączne unread cells; wpisujemy
tam te konkretne words w little endian, a skipped/discarded cells zostawiamy
dowolne. Jest to rozszerzenie każdej zgodnej finite PAST, bez zmiany dawnych
bytes. Nowe block może być wybrane tak samo; mathematical witness nie zależy
od realnego generatora. W rezultacie istnieje rzeczywisty legalny finite scalar
prefix bez fault, realizujący każdy dopuszczony y. Source positive atoms mają
nawet w_y≥2^-264 i K_h(y)=w_y/A_h≥2^-264.

## 3. Indukcja source realizability i domains

Invariant po i completed returns: h∈Hi ma finite actual source witness od E;
caller projection V_e(h) i ramy pamięci są równe source, faultNONE. Każdy
ukończony terminal pair ma actual source-bank/width/correlation budget, a
read-time snapshots następnego current subprogramu są initialized i defined.

Base: E i odebrany ORDERED forward initial path/split contracts dają pierwszy
current NumericCenter. Intercall operations poprzedzające go są objęte source
primitive domains; nie wykonujemy unsafe floor, aby dopiero zmierzyć jego input.

Step: z h i jego already-proved current domain budujemy witness z §2. Source
return y jest normal, signed32-safe, ZERO residual bound jest teraz stosowalny.
Dołączamy deterministic caller instructions do następnego cutu. Przez frame
ich słowa nie zależą od wybranego witness/rejection count. ORDERED/LEFT
forward source contracts wyprowadzają ich definedness i następny NumericCenter.
Nie jest potrzebne istnienie nieskończonej/pełnej terminating przyszłej trace.
Lean SupportPrefix/support_realizes formalizuje induction logic; konkretna
source instancja jest poniżej i w odebranych pinned analytical proofs.

### Prawa gałąź i terminal pair

ORDERED/INVARIANT §§2–4 daje signed primitive/split domains i
|mu|≤156276714 dla wszystkich legal normal finite histories prawej branch.
Na terminalu pierwszy current mu1 jest legalny PRZED jego floor; po normal Y1
r1<367 i rx<184. Dopiero wtedy updated mu0 ma udowodniony bound przed drugim
floor; po normal Y0 returned residual caps wynoszą552/367. To nie dwa
niezależne residuals i nie samo globalne |Y−mu|≤367.

NORMALIZED wiąże stored i paired sigma z TYM SAMYM actual stable D. First-bank
success/failure inequalities (dss≥a_j i dss<a_(j−1)) dają każdorazowy
D*residual² bound w LEFT/BANK_WEIGHTED_BOUNDS.json. Source half i last sub
mają defect≤2^-37; identity
Q_A2(r0,r1)=(normal_residual0+defect)^2+(3/4)normal_residual1² zachowuje
paired factor3/4 i cross error. Każdy normal pair spełnia
**D Q_A2≤849346588**, niezależnie od joint law.768 pairs dają652298179584.
Wszystkie witnesses używają actual bank i actual updated second center, więc
ten correlated budget przenosi się na KAŻDĄ gałąź support reference.

### Root transfer i lewa gałąź

Po completed right witness LEFT/RIGHT_RESIDUAL_ENERGY rekonstruuje jego actual
residual z tych samych terminal pairs/raw L; stable/raw metric factor6 daje
energy≤3913789077504, source reconstruction defect
20102235062439/562949953421312. Root CM/add ma defined operands i correlated
U frequency cap20099472117, inverse-evaluation coefficient cap786308210.
Nie zastępujemy L idealnym LDL, nie używamy niezależnych uniform residual boxes.

LEFT/LEFT_INVARIANT prowadzi signal/history deviations w actual order; terminal
mu1, potem updated mu0 uzyskują |mu|≤937866518 przed odpowiednimi instrukcjami.
Podane current domain i NORMALIZED scalar arithmetic wykluczają guards fault.
Wszystkie intercall signed operations pozostają w cap2^100. Repeated CM ma te
same bytes przez immutable-L/residual frame; rounded sub ma własny error.

Sticky fault nie jest hidden future premise: base maNONE; current domains
wykluczają NOWY fault przy każdym finite scalar step; witness i caller frame
zachowująNONE. Infinite rejection nie daje caller stores, lecz nie wymaga
też uzasadniania następnego nieosiągniętego cutu.

## 4. Konsekwencje dla referencji

Każdy finite support prefix w Q_S oraz live Q_stop ma actual normal witness;
zatem wszystkie source-domain/terminal correlation premises pozostają ważne.
Po M returns istnieje legalny root residual i POST domain. Po y∉S_h nie
konstruujemy tego witness i NIE wykonujemy source update; Q_stop zwraca EXIT.
Brak support witness nie jest sam w sobie dowodem NumericCenter failure/UB.

Historyczne ORDERED pozostaje PARTIAL_PROOF, LEFT domyka wskazany w nim brak.
Nowy theorem kompozycyjny konsumuje oba checkpointy bez zmiany ich bytes/statusów.
Nie podnosimy syntetycznych native trees lub standalone witnesses do Emitted.
