# Referencja, zdarzenie i warunkowanie

Fix required emitted/canonical normalized root entry e i legal entry PAST
w IID_BUFFER. JOINT/REFERENCE_PROCESSES definiuje ten sam deterministic
source state transition na każdym legal value prefix. Wybieramy Q_S:
Y_i|h∼G_(val(mu_h),val(sigma_h)^2) conditioned on S_h={y:K_h(y)>0}.
To local normalization1/(1−t_h),t_h≤tau z GAUSS/JOINT. Q_S ma dokładnie3072
live scalar draws. Positive-prefix closure konsumuje źródłowe domains i
fault exclusion przed dalszymi instructions oraz przed literalnym POST.

Nie utożsamiamy Q_S z product law niezależnych marginals,globalną lattice
Gaussian lub Q_stop conditioned on whole-call survival. Mean/width parameters
są true values actual words na TYM SAMYM history h, nie Ghat/dss parameters.
PAST nie ujawnia unread buffer/future tape,nie zawiera future norm/Sign success.

```
Safe16(w1,w2) := ∀i<1536,
  -32768≤w1_i≤32767 AND -32768≤w2_i≤32767;
B_e := {Live(Y):not Safe16(Phi_e(Y).w1,Phi_e(Y).w2)}.
```

w1,w2 to int64 results actual source rint przed casts1931–1932. Stored int16
values i ich późniejsza norma nie definiują tego eventu. Oba vectors są objęte
joint eventem; nie wybiera się tylko zaakceptowanego normą subsetu.

Na EXIT w Q_stop nie ma suffixu/w1/w2. B wyklucza ten tag. Odebrana identity
Q_stop(Live y)=Q_S(y)Π_i(1−t_hi)≤Q_S(y) daje Q_stop(B)≤Q_S(B), bez dodatkowego
support-exit kosztu i bez całkowitego survival conditioning. B∪EXIT byłoby
innym zdarzeniem. Main numeric q dotyczy Q_S, jego scope pozostaje jawny.

Uniform bound kwantyfikuje po każdym takim e i PAST, nie po jednym synthetic
tree. Użycie istniejącego emitted stable gate nie dodaje go do P_key;
broad-P_key narrow-gate acceptance gap pozostaje poza tezą.
