# Forward closure wszystkich left ACTIVE_PRE_FLOOR centers

## 1. Signal/history decomposition

Na entry do source SplitTop1828 U oznacza ACTUAL source-word values po root
add, nie idealny target. Niech cU=mathematical inverse-evaluation(U). ROOT_TRANSFER
daje |(cU)_i|≤B=786308210 i frequency cap M=20099472117.

W każdym current target prowadzimy jego exact SIGNAL: coefficient grouping
cU według actual top/deep path, oraz HISTORY/ROUNDING deviation H. Exact
split maps są coefficient permutations; signed source splitters mają
osobne error boundy. Przed terminal Split1 każdy pure-signal complex norm≤M
(contraction normalized coefficient extraction); na base0 exact signal to
jedna coefficient cU, więc scalar bound≤B. Nie utożsamiamy mathematical
inverse-eval ze źródłowym split lub przyszłym iFFT.

## 2. Jawna recurrence source domains

Γ=2^-36,tau=2^-800. Dla target o deviation H:
- source split k≥2 lub top: H'≤H+Γ(M+H)+tau;
- terminal Split1: H'≤2H+Γ(M+H)+tau;
- source update target+P, gdzie ACTUAL product ma proved cap P:
  H'≤H+P+Γ(M+H+P)+tau.

Exact arithmetic/outward2^-50 rounding każdego kroku jest w energy_transfer.py.
Product/residual caps są tymi samymi source-forward contracts co ORDERED,
lecz używane wyłącznie po wcześniejszych domains. W binary najpierw oblicza
się right subtree, potem jego returned z1→product→updated left. W cubic
kolejność2,1,0, z zachowanymi różnymi subtraction snapshots. Source guards/
statements ani parametry nie są zmienione.

## 3. Terminal bez koła

Na base0 pierwsze mu1 ma cap B+H1. Certyfikat sprawdza jego NumericCenter
margin PRZED użyciem scalar result. Po actual NORMAL_RETURN wolno zastosować
ZERO i bound r1<367,rx<184. Dopiero wtedy nowe mu0 ma cap
`B+H0+184+Γ(B+H0+184)+tau`; sprawdzamy go przed drugim ZERO consumerem.
Po drugim normal return returned r0/r1 mają caps552/367. Są residuals,
nie sampled integers. Half/signed-zero/last subtraction błędy są zachowane.

Wymagane calls występują wyłącznie w legal finite histories. Gdy right child
nie wraca, parent nie przechodzi do updated targetu. Gdy nastąpił fault,
source sticky flag wyklucza następny ACTIVE_PRE_FLOOR. Nie zakładamy globalnie
fault-free trace i nie ograniczamy liczby proposals. Można więc indukcyjnie
konsumować NORMAL_RETURN tylko dla już zakończonych dzieci odpowiedniego
aktywnego prefixu, nie zakładając zakończenia wszystkich rejection loops.

## 4. Wszystkie paths i wynik

Odwiedzono768 terminal blocks lewej gałęzi (1536 source scalar positions),
z osobnymi H0/H1 i mu1/mu0 records. Każdy miał sprawdzony current margin
przed scalar consumerem. Otrzymany uniform bound:

```
finite(mu), |val(mu)| <=937866518,
lower margin1209616765, upper margin1209616764.
```

Root U frequency cap<2^35, current history caps<2^31, więc wszystkie
intercall primitive operands/products/sums pozostają wewnątrz2^100 przed
wykonaniem. Magnitude-based contracts obejmują signed zero/cancellation i
half-created subnormals. Nie założono dodatniości targetów.

To source proof dla każdego required emitted/canonical entry i każdego
left active finite prefixu po warunkowo ukończonej prawej gałęzi z faultNONE.
Per-path numerical certificate instancjuje induction; same testowe3072
positions lub wpisane constants w Lean nie są jego uzasadnieniem.
