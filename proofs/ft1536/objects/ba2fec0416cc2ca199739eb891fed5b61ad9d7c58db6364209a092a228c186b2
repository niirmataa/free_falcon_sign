# Zachowane niezamknięte strategie — klasyfikacja

## 1. Frequency cap jako scalar cap

TARGETS t0<4829216911 przekracza2^31. To nie są centers po split/update.
Przyjęcie takiej samej normy do całej recursion nie zamyka NumericCenter,
ale nie wskazuje wadliwego C inputu. Klasyfikacja: **za luźna majoranta**.

## 2. Niezależne L supnorms

bounds.py wykonał dwa ordered bound runs: stare lower binary L<2 oraz
refinement |L|<1+2^-40 z actual positive H. Oba obejmują pełną globalną
normal-return support[-365,366], także zero/cancellation roundoff. Refinement
zamyka pierwszą root branch do |mu|≤156276714. Root transition z L<2^25
daje jednak updated-target bound3864968087959271. Kontynuowana kandydacka
majoranta to7729936365272004 — nie jest dowodem dalszych calls, gdyż wymagałaby
ZERO na jeszcze niewykazanych center domains. Klasyfikacja: **failed bound
route**, nie source/emitted counterexample.

## 3. Sam mathematical inverse-eval norm

TARGETS ideal/reference coefficient bound173861259265/36866<5000000 jest
użyteczny, ale nie kontroluje dodatkowego source CM(z1,Lroot) ani wszystkich
source split/merge errors. Nie zidentyfikowano inverse-eval z wykonywanym
splitterem. Sama ta norma nie jest brakującym source invariantem.

## 4. Weighted/correlated refinament — obiecująca, jeszcze OPEN instancja

Zbadano idealną T5/A2 geometrię. Jest ona twierdzeniem o exact LDL i exact
integral splitting; nie dowodzi identity dla raw machine L po nadpisaniu
leaves przez stable rebuild. Nie przeniesiono dawnego>991/RN premise na C.

Bank-specific support29/59/118/235/365 jest silniejszy niż uniwersalne365:
po lower-bound na selected variance można oszacować residual²*stableD.
Exact exploratory budgets są w bounds.json; maksymalny candidate R²D to
424673294 zamiast stosowania366²*Dmax wszędzie. W IDEAL scale geometry daje
to root-correction candidate275528238, ze sporym arytmetycznym zapasem.

Ta liczba NIE jest exported source mu bound. Brakują:
1. source selected-bank inverse-bound/error instancja we właściwej metric;
2. uniform raw-L/stable-D Gram comparison (w tym imaginary/split defects);
3. ordered returned-residual energy i cumulative source add/sub errors,
   z input-preservation dla recomputed products;
4. perturbation transport actual root L/D do ideal root gain i dalszych
   partial left centers, nie wyłącznie końcowego residual norm.

Sprawdzony local L refinement nie wystarcza do tej globalnej relacji. Gdyby
po prostu założyć raw L=ideal stable LDL, proof ominąłby właśnie wymagany
obowiązek. Zachowujemy go jako konkretny następny typ, nie aksjomat celu.

## 5. Co mówią negatywne kontrole

Wrong-order/stale-mu/fault-as-close/nonreturn-as-zero/Split1/final-sub/snapshot
mutations są wykonanymi zmianami modelu z pełnymi receipts. Unsafe synthetic
base mu=2^40 zatrzymano przed floor; fixture nie spełnia required TARGETS
entry, więc to EXTENDED test-domain witness, nie emitted counterexample.
Typical support cap3 jest odrzucony przez rzeczywistą bank4 support365,
bez twierdzenia o prawdopodobieństwie danego źródłowego tape.

Nie znaleziono ani nie ogłoszono błędu C w wymaganej domenie. Główny rezultat
to **PARTIAL_PROOF**: istotny source-safe prawy prefix i dokładna blokada
matematyczna lewej części. P_key/Emitted/cel i kod nie zostały dopasowane.
