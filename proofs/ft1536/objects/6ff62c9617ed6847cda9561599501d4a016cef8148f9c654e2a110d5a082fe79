# Następne osobne obowiązki po pełnej remaining binary tower

Wyjście bieżącego etapu: wszystkie S_l,l7..1 i total terminating original
inner7 dla12 entry groups; raw trees z10752 internal L i1536 positive real
leaf words. Powiązania i offsets są w ASSEMBLY_INTERFACE, uniform constants
i path-specific records w TOWER_CERTIFICATE. To nie normalized widths.

## Najbliższy pełny typ — raw loader/tree assembly, jeszcze OPEN

Key4/P_key jak ROOT. LegalRawExpansionBuffers określa rzeczywiste allocations
load_skey (B/tree i tmp), disjointness, rozmiary i lifetime. RawPrefix_C to
wykonanie oryginalnego load_skey do chwili POWROTU ffLDL_fft3, przed stable
rebuild/normalize; nie jest runtime patchem lub nowym API.

```
forall E,sk,pk,p,memory,
 Emitted_C(E,sk,pk) -> SourceDecodeSameSTATIC(sk)=p ->
 LegalRawExpansionBuffers(memory,p) ->
 TerminatesDefinedAtRawPrefix_C(p,memory) and
 BasisWords=SourceFFT_B(p) and
 RawTreeWords=Assemble(
   RootSlice_C(p), Node3Slices_C(p), Node2Slices_C(p),
   RemainingInner7Trees_C(p)) and
 RawTreeLength=18432 and
 All16896InternalWordsFinite and All1536RawLeavesPositiveFinite and
 CorrectSourceOrderAndFrame(memory).
```

E jest legalnym M0 environment, sk/pk finite byte strings, p four Int^1536
vectors tego samego sk. Assemble ma konkretne offsets/read moments opisane
w ASSEMBLY_INTERFACE, nie dowolną permutację lokalnych certyfikatów.
Current tower theorem jest silnym inputem, ale tego całego prefix bindingu
nie ogłoszono proved. Branch0 root/cubic/level8 order i scratch reuse trzeba
skomponować z wcześniejszymi etapami jako jedno theorem.

## Następny po assembly typ — stable rebuild/normalization, również OPEN

```
forall p,memory, P_key(p) -> RawPrefixCertificate(p,memory) ->
 DefinedTerminating(ft_build_stable_certified_leaves;
                   ffLDL_ternary_normalize) and
 leaf_count=1536 and tree_words=18432 and
 InternalLWordsPreserved and
 StoredTerminalWidths=SourceNormalizedStableWidthSequence(p,sigma=768) and
 AllRequiredWidthDomainsAndGates.
```

Źródłowe sqrt/inverse/scaling/gates, real stored sequence i consumed
historical H4 premises mają własne domain obligations. Nie zastępuj ich
nazwą theorem ani complete-IEEE premise. Audyt FPEMU jest w osobnej sesji;
bieżąca praca go nie uruchamiała ani nie czytała jego aktywnego W.

INITIAL_TARGETS: source FFT3(c), basis products i inverse(q), osobno.
ORDERED_REACH: emitted/M0 prefix→NumericCenter przed użyciem ZERO_SCALAR,
także pre-dss/norm-rejected attempts. Fault0 nie ma residual-closeness.
Sampler law, distribution/acceptance/hybrid losses i M7 pozostają oddzielne.
Nie nadano CT/security claim, nowego abortu lub automatycznej globalnej straty.
