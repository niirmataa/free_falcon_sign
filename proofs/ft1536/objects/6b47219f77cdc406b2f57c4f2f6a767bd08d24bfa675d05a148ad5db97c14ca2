# Standardowy replay NODE2

Pre-freeze rehearsal PASS160/160, semantic pin:
`84111b87b8b5dee95862769ede05c1d27b843e4e30c2ef6e1479aa77236dfbbd`.
To osobna kotwica bez hash cycle, nie finalny OUTPUTS pin.

```sh
python3 -B scripts/replay.py /ABSOLUTE/COPY/tmp/new-run EXTERNAL_OUTPUTS_SHA256
```

External pin i WSZYSTKIE OUTPUTS members oraz safe paths/symlinks/duplicates
i binding matches sprawdzane są PRZED nowym DEST. Nie kopiuje się olean/
bin/cache. Jobs rebuild26 Lean modules, normal/sanitizer C, kernel half values,
QQ/RBF certificates/oracle, source binding, ledger i toolchain. Inputs/slices
są generowane ponownie. Porównuje się wszystkie160 matches, nie sam PASS.

Bootstrap/source readonly, HOME/TMPDIR/DOT_SAGE/LEAN_PATH/cache pod DEST;
skończone wall/CPU limits≤240s, normal8GiB, Lean -j1 -M2048, oddzielny ASan
shadow mode. Parent controller nie może odziedziczyć twardego8GiB.
Nie trzeba starego W, Dokumenty/H/USB lub worktree. Pełne rehearsal streams
i receipts są zarchiwizowane pod artifacts/rehearsal i objęte OUTPUTS.

Piny tekstów ANALYTIC_PROOF/UPSTREAM_REFINEMENT/NODE2_FRAME nie są kernelowym
sprawdzeniem ich całego argumentu. Mixed proof scope i level9→8 są zachowane
w każdym REPLAY_RESULT. Finite C/oracle controls nie zastępują universality.

Po freeze:
```sh
python3 -B scripts/verify_final.py EXTERNAL_OUTPUTS_SHA256
python3 -B scripts/post_freeze.py /ABSOLUTE/COPY/tmp/final_standard_001 EXTERNAL_OUTPUTS_SHA256
python3 -B scripts/verify_final.py EXTERNAL_OUTPUTS_SHA256
```
Kontroler montuje frozen W read-only, zapis tylko pod tmp, ukrywa Dokumenty/
stages i zachowuje DEST/POST_FREEZE.json wraz z pełnymi streams. Ten sam
manifest jest weryfikowany ponownie. Frozen report/result/manifest nie są
aktualizowane po standardzie; receipt znajduje się w nowym DEST.
