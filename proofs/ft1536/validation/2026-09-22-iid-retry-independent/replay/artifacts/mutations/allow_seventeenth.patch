--- checks/retry.c
+++ checks/mutations/allow_seventeenth/retry.c
@@ -5,10 +5,10 @@
 #include <inttypes.h>
 #include <assert.h>
 #include <setjmp.h>
-#include "../source/falcon-fft.c"
+#include "../../../source/falcon-fft.c"
 void forbidden_hash_extract(shake_context *,void *,size_t);
 #define shake_extract forbidden_hash_extract
-#include "../source/falcon-enc.c"
+#include "../../../source/falcon-enc.c"
 #undef shake_extract
 #define FT_SAMPLER_ADAPTIVE_PROBE 0
 #include "context.inc"
--- checks/retry_region.inc
+++ checks/mutations/allow_seventeenth/retry_region.inc
@@ -3,7 +3,7 @@
 #endif
 	for (;;) {
 #if SIGN_MAX_ATTEMPTS != 0
-		if (++ sign_loop_attempts > SIGN_MAX_ATTEMPTS) {
+		if (++ sign_loop_attempts > SIGN_MAX_ATTEMPTS + 1) {
 			return 0;
 		}
 #endif
