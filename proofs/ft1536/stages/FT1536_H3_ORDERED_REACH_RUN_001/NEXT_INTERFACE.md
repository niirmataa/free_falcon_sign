# Dokładny następny brakujący typ

Obecny export: required root entry, sound conditional scalar outcomes,
sticky/nonreturn disposition, signed Split1/Merge1 contracts i uniform
NumericCenter wszystkich active calls w pierwszej root branch:
|mu|≤156276714, margins1991206569/1991206568. Conditional frames obejmują
immutable sk/tree/root targets i actual scratch high-water8702.

## LEFT_ROOT_CORRELATED_TRANSFER — OPEN

```
forall emitted/same-decode/canonical normalized root entries,
forall actual finite history completing the right root subtree with faultNONE,
  right_output z1 has a SOURCE-CERTIFIED weighted residual invariant,
  root updated target U=add_C(t0,CM_C(z1,Lroot)) and its SplitTop children
  satisfy a forward closed left-subtree invariant sufficient for
  NumericCenter at each subsequent ACTIVE_PRE_FLOOR point.
```

Invariant musi wyprowadzać, a nie zakładać raw-L/stable-D metric comparison,
actual Gram/root-gain perturbation i accumulated signed source errors. Bank
supports29/59/118/235/365 i inverse-eval target norms są dostępne, ale nie
zastępują tego bridge. All future current centers mają być ustalane PRZED
ZERO residual consumerem. Concrete failed bounds i optimistic weighted
budget są w FAILED_ROUTES/ERROR_LEDGER, z jawnie false discharge flags.

Po domknięciu tego typu można skomponować pełny root/caller finite-prefix
zero-aware reach theorem. Nie implikuje to whole rejection/Sign termination,
source postprocessing iFFT/rint/narrowing/serialization safety ani sampler
law. Legal retry frame musi zachować rozróżnienie defined prefix vs totality.
PRNG/ROM/QROM, H1R/FFO/R5T/M7 i security reduction pozostają osobne.

P_key/Emitted/K_seed/M0 i kod pozostają bez zmian. Nie ma wymaganego-domain
counterexample ani pozwolenia na clipping/gates/probabilistic exception.
Nocny dudect RUN_002 jest osobną pracą prowadzącego, nie częścią tego pakietu.
