# Entry do ORDERED_REACH po INITIAL_TARGETS

INITIAL_TARGET_CERTIFICATE eksportuje na candidate56974571…:
- legal emitted/same-STATIC/normalized entry i WSZYSTKIE canonical c;
- actual defined terminating cut przed ffSampling_fft3:1897;
- t0/t1 exact source words w tmp0..3071, ni3f0c7161fb1566d0;
- C FFT physical map768 complex slots, error component<1/8192;
- rounding-only errors t0<1/8192,t1<1/16777216 do actual word-value products/q;
- ideal errors t0<1/4,t1<1/8192 do eval(-cF/q),eval(cf/q);
- source norm/component upper t0<4829216911,t1<2359169;
- coefficient/reference norms i mathematical inverse-eval error transport;
- niezmieniony cały normalized sk24576 words, hm, outputs/context i tmp suffix.

## Konkretny następny entry type

```
RootSamplingEntry(p,c,memory):
  InitialTargetCertificate(p,c,memory)
  and NormalizedExpansionCertificate(p,sk)
  and root call arguments:
    z0=tmp+3072, z1=tmp+4608, tree=sk+6144,
    t0=tmp, t1=tmp+1536, logn=10, scratch=tmp+6144
  and required live/aligned/disjoint output/scratch/context buffers.
```

To są formed pointers, nie already initialized przyszłe outputs. Warunki
required sampling-frame trzeba doprowadzić w rzeczywistym callerze także
przez historię poprzednich attempts.

## ORDERED_REACH — OPEN

```
forall legal emitted/M0 prefixes entering root sampling,
  through the actual right-before-left recursion and residual updates,
  before EACH reachable scalar floor/cast/residual(mu),
    NumericCenter(mu) and RequiredPrimitiveDomainsAndFrame.
```

Nowy proof musi prowadzić current targets/snapshots, residuals, accumulated
errors, rejection/fault/coins histories i aborted/norm-rejected attempts.
Nie wolno utożsamić frequency target z późniejszym scalar center lub użyć
ZERO_SCALAR do wyprowadzenia jego premise. Duża majoranta frequency t0 nie
jest sama w sobie scalar-center counterexample. Mathematical inverse-eval
norms nie są jeszcze source split/update bounds.

Normalized sigma-only stored/paired/dss certificate zachowuje swój scope;
nie dowodzi earlier mu prefixu. Sampler-law/H1R/FFO/R5T/M7, complete Sign
termination, security reduction i full CT pozostają osobnymi celami.
Na tym handoffie nie rozpoczęto ORDERED_REACH.
