--- inputs/bootstrap/H6P/scripts/run.py
+++ scripts/run.py
@@ -1,9 +1,9 @@
-"""Bounded H6P jobs: one worker, durable W-only, RO inputs/source, network off."""
-import datetime,fcntl,hashlib,json,os,resource,signal,subprocess,sys,time
+"""Single bounded RETRY proof worker; durable W only, input/source RO, network off."""
+import datetime,fcntl,hashlib,json,os,resource,signal,subprocess,sys,time,shutil
 from pathlib import Path
 W=Path(__file__).absolute().parents[1];limit=int(sys.argv[1]);argv=sys.argv[2:];assert argv and 1<=limit<=240
-asan=os.environ.get('FT1536_ASAN')=='1'
-for r in ['logs','artifacts','tmp','cache/home','cache/sage','cache/mpl','cache/ipython']:(W/r).mkdir(parents=True,exist_ok=True)
+asan=os.environ.get('FT1536_ASAN')=='1';disk=shutil.disk_usage(W);assert disk.free>1024**3
+for r in ['logs','artifacts','tmp','runtime','cache/home','cache/sage','cache/mpl','cache/ipython']:(W/r).mkdir(parents=True,exist_ok=True)
 env=dict(os.environ);env.update(HOME=str(W/'cache/home'),TMPDIR=str(W/'tmp'),TMP=str(W/'tmp'),TEMP=str(W/'tmp'),XDG_CACHE_HOME=str(W/'cache'),DOT_SAGE=str(W/'cache/sage'),MPLCONFIGDIR=str(W/'cache/mpl'),IPYTHONDIR=str(W/'cache/ipython'),PYTHONDONTWRITEBYTECODE='1',PYTHONOPTIMIZE='0',MAMBA_ROOT_PREFIX='/home/footfalcon/miniforge3',GIT_OPTIONAL_LOCKS='0',OMP_NUM_THREADS='1',OPENBLAS_NUM_THREADS='1',LEAN_PATH=str(W/'formal'),ASAN_OPTIONS='detect_leaks=0:abort_on_error=1',UBSAN_OPTIONS='halt_on_error=1:print_stacktrace=1',FT1536_HOST_NET_NS=os.readlink('/proc/self/ns/net'))
 box=['/usr/bin/bwrap','--die-with-parent','--unshare-net','--unshare-pid','--ro-bind','/','/','--bind',str(W),str(W),'--proc','/proc','--dev','/dev','--chdir',str(W)]
 for r in ['inputs/bootstrap','source']:
@@ -22,6 +22,6 @@
   r='logs/'+tag+'.'+k
   with (W/r).open('xb') as f:f.write(data)
   paths[k]=r
- row=dict(start_utc=start,cwd=str(W),argv=argv,sandbox=box,exit_code=p.returncode,timeout=timed,wall_limit_seconds=limit,cpu_limit_seconds=limit,address_space_bytes=None if asan else 8*1024**3,asan_shadow_reservation=asan,elapsed_seconds=time.monotonic()-t,**paths,stream_sha256={'stdout':hashlib.sha256(o).hexdigest(),'stderr':hashlib.sha256(e).hexdigest()},environment={k:env[k] for k in ['HOME','TMPDIR','DOT_SAGE','XDG_CACHE_HOME','LEAN_PATH','GIT_OPTIONAL_LOCKS','PYTHONOPTIMIZE']})
+ row=dict(start_utc=start,cwd=str(W),argv=argv,sandbox=box,exit_code=p.returncode,timeout=timed,wall_limit_seconds=limit,cpu_limit_seconds=limit,address_space_bytes=None if asan else 8*1024**3,asan_shadow_reservation=asan,elapsed_seconds=time.monotonic()-t,disk_free_before=disk.free,**paths,stream_sha256={'stdout':hashlib.sha256(o).hexdigest(),'stderr':hashlib.sha256(e).hexdigest()},environment={k:env[k] for k in ['HOME','TMPDIR','DOT_SAGE','XDG_CACHE_HOME','LEAN_PATH','GIT_OPTIONAL_LOCKS','PYTHONOPTIMIZE']})
  with (W/'COMMANDS.log').open('a') as f:f.write(json.dumps(row,sort_keys=True)+'\n')
 sys.stdout.buffer.write(o);sys.stderr.buffer.write(e);sys.exit(124 if timed else p.returncode)
